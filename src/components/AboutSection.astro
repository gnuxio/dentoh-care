---
const achievements = [
    { icon: "graduation", title: "Especialista Certificada", description: "Universidad Nacional" },
    { icon: "award", title: "Premios de Excelencia", description: "Reconocimiento profesional" },
    { icon: "users", title: "5000+ Pacientes", description: "Sonrisas transformadas" },
    { icon: "heart", title: "Atención Integral", description: "Cuidado personalizado" },
];

// Icons como STRINGS (NO JSX)
function iconSvg(name) {
    switch (name) {
        case "graduation":
            return `
        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" stroke-width="2"
             viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round"
            d="M12 14l-9-5 9-5 9 5-9 5zm0 0v7m4-4a4 4 0 11-8 0" />
        </svg>
      `;
        case "award":
            return `
        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" stroke-width="2"
             viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round"
            d="M12 8l3 3-3 3-3-3 3-3zm0 13l3.09-3.09a6 6 0 10-6.18 0L12 21z" />
        </svg>
      `;
        case "users":
            return `
        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" stroke-width="2"
             viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round"
            d="M17 20v-2a4 4 0 00-4-4H7a4 4 0 00-4 4v2m14-8a4 4 0 10-8 0 4 4 0 008 0z" />
        </svg>
      `;
        case "heart":
            return `
        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" stroke-width="2"
             viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round"
            d="M4.318 6.318a4.5 4.5 0 016.364 0L12 7.636l1.318-1.318a4.5 4.5 0 116.364 6.364L12 21 4.318 12.682a4.5 4.5 0 010-6.364z" />
        </svg>
      `;
    }
}
---

<section id="sobre-mi" class="py-32 bg-white relative overflow-hidden">
    <div class="absolute top-0 right-0 w-96 h-96 bg-teal-100/30 rounded-full blur-3xl"></div>

    <div class="max-w-7xl mx-auto px-6 lg:px-8 relative z-10">
        <div class="grid lg:grid-cols-2 gap-16 items-center">

            <!-- Left image -->
            <div class="relative fade-on-scroll opacity-0 -translate-x-8">
                <div class="relative rounded-3xl overflow-hidden shadow-2xl">
                    <img
                            src="https://images.unsplash.com/photo-1559839734-2b71ea197ec2?w=800&q=80"
                            alt="Dra. Ana García"
                            class="w-full h-[600px] object-cover"
                    />
                    <div class="absolute inset-0 bg-gradient-to-t from-teal-900/30 to-transparent"></div>
                </div>

                <div class="absolute -bottom-6 -right-6 bg-gradient-to-br from-teal-500 to-cyan-500 rounded-2xl shadow-2xl p-8 text-white max-w-xs">
                    <div class="text-5xl font-bold mb-2">15+</div>
                    <div class="text-lg">Años dedicados a crear sonrisas perfectas</div>
                </div>
            </div>

            <!-- Right content -->
            <div class="space-y-8 fade-on-scroll opacity-0 translate-x-8">
                <div>
                    <div class="inline-block px-4 py-2 bg-teal-100 rounded-full text-teal-700 font-medium text-sm mb-4">
                        Conoce a tu Dentista
                    </div>
                    <h2 class="text-4xl lg:text-5xl font-bold text-gray-900 mb-6">
                        Dra. Ana García
                    </h2>
                    <div class="text-xl text-teal-600 font-medium mb-6">
                        Especialista en Odontología Estética e Integral
                    </div>
                </div>

                <div class="space-y-4 text-gray-600 text-lg leading-relaxed">
                    <p>
                        Con más de 15 años de experiencia, me dedico a transformar sonrisas y mejorar
                        la calidad de vida de mis pacientes usando tratamientos dentales de última
                        generación.
                    </p>
                    <p>
                        Mi filosofía se basa en la prevención, el cuidado personalizado y el uso de
                        tecnología avanzada para garantizar resultados excepcionales.
                    </p>
                    <p>
                        Cada paciente es único, y diseño planes personalizados según sus necesidades
                        y objetivos de salud dental.
                    </p>
                </div>

                <!-- Achievements -->
                <div class="grid grid-cols-2 gap-6 pt-8">
                    {
                        achievements.map((item, index) => (
                                <div
                                        class="bg-gradient-to-br from-teal-50 to-cyan-50 rounded-2xl p-6 hover:shadow-lg transition-shadow fade-on-scroll opacity-0 translate-y-6"
                                        style={`transition-delay: ${index * 100}ms`}
                                >
                                    <div
                                            class="w-12 h-12 bg-gradient-to-br from-teal-400 to-cyan-500 rounded-xl flex items-center justify-center mb-4"
                                            set:html={iconSvg(item.icon)}
                                    ></div>

                                    <h3 class="font-bold text-gray-900 mb-1">{item.title}</h3>
                                    <p class="text-sm text-gray-600">{item.description}</p>
                                </div>
                        ))
                    }
                </div>
            </div>
        </div>
    </div>
</section>

<script>
    const elements = document.querySelectorAll(".fade-on-scroll");

    const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
            if (entry.isIntersecting) entry.target.classList.add("fade-in-visible");
        });
    }, { threshold: 0.2 });

    elements.forEach((el) => observer.observe(el));
</script>

<style>
    .fade-in-visible {
        opacity: 1 !important;
        transform: translate(0, 0) !important;
        transition: all 0.8s ease-out;
    }
</style>